import { LambdaLog } from 'lambda-log';
export declare const REGION: string, S3_BUCKET_NAME: string, REKOGNITION_MIN_DOCUMENT_CONFIDENCE: number, REKOGNITION_MIN_TEXT_CONFIDENCE: number, CUSTOM_MIN_INE_WORD_CONFIDENCE: number, CUSTOM_MIN_INE_PHRASE_CONFIDENCE: number, CUSTOM_MIN_PASSPORT_WORD_CONFIDENCE: number, CUSTOM_MIN_PASSPORT_PHRASE_CONFIDENCE: number, PROJECT_NAME: string, STAGE: string, DYNAMO_DB_TABLE_NAME: string;
export declare const logger: LambdaLog;
